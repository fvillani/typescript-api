import {Core} from "./core";

/**
 * @summary Model de videos
 * @type url {String} url do video
 * @type duração {String} tempo do video
 * */
export class Videos extends Core {
    url: String;
    duracao: String;
}